# Client C 配置 - 使用 QUIC 协议
client_id: 0  # 0 表示匿名模式
auth_token: ""
anonymous: true
device_id: "test-client-c"

server:
  address: "localhost:7003"
  protocol: "quic"

# 端口映射配置（通过 Management API 动态配置）

